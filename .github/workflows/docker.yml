name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Login to Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.gayatribajaj }}
          password: ${{ secrets.Gayupict02@ }}

      # 3. Build Docker image (force new image every time)
      - name: Build Docker image
        run: |
          docker build \
            --build-arg CACHE_BUST=$(date +%s) \
            -t ${{ secrets.gayatribajaj }}/webapp:latest .

      # 4. Push Docker image
      - name: Push Docker image
        run: |
          docker push ${{ secrets.gayatribajaj }}/webapp:latest